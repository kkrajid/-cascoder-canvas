{
  "name": "@cascoder/canvas-monorepo",
  "private": true,
  "version": "0.1.0-alpha.1",
  "description": "The React Design Editor Engine — build Canva-like editors in your app",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "clean": "turbo run clean",
    "playground": "pnpm --filter @cascoder/playground dev"
  },
  "devDependencies": {
    "turbo": "^2.4.0",
    "typescript": "^5.7.0"
  },
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=20.0.0"
  }
}
